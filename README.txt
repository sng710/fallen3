GitHub Pages fixed version

Upload everything in this folder to the root of the fallen3 repository.

Important fix in this version:
- The background video is embedded inside index.html as a data video, so it does not depend on a separate video path.
- field.mp4 is still included as an extra fallback.
- The background is transparent-safe, so the page background cannot cover the video.
- Personal cards do not use familyGroupPhoto as the private image.

After upload, wait 1–3 minutes and press Ctrl+F5.
